{
  "name": "github-runner-microvm-app",
  "private": true,
  "description": "Lifecycle hook server baked into the MicroVM image (app.js). Installed inside the Docker image with npm install --omit=dev, not in the Lambda deployment package.",
  "dependencies": {
    "@aws-sdk/client-lambda-microvms": "^3.1116.0"
  }
}
